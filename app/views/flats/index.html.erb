<h1>Amazing Flats to rent</h1>
<%= link_to "Create new flat", new_flat_path %>
<p>All our <%= @flats.count %> flats here. Pick one!</p>
<div class="container">
  <% @flats.each do |flat| %>
    <div class="card">
      <h3><%= flat.name %></h3>
      <p><%= flat.address %></p>
      <div class="info">
        <p>💁<%= flat.number_of_guests %></p>
        <p>💲<%= flat.price_per_night %></p>
        <p>🛌<%= flat.number_of_rooms %></p>
      </div>
      <%= link_to "More info", flat_path(flat) %>
    </div>
  <% end %>
</div>
